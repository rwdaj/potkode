apiVersion: v1
kind: Service
metadata:
  name: sodsaul
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "sodsaul"
    port: 8080
  selector:
    app: sodsaul
