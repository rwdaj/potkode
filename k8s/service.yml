apiVersion: v1
kind: Service
metadata:
  name: grbeof
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "grbeof"
    port: 8080
  selector:
    app: grbeof
