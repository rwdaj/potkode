apiVersion: v1
kind: Service
metadata:
  name: tergos
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "tergos"
    port: 8080
  selector:
    app: tergos
