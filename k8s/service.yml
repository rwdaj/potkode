apiVersion: v1
kind: Service
metadata:
  name: korizon
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "korizon"
    port: 8080
  selector:
    app: korizon
