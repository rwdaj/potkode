apiVersion: v1
kind: Service
metadata:
  name: bocprov
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bocprov"
    port: 8080
  selector:
    app: bocprov
